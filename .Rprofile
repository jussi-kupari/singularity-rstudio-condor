# Ensure project R library is first in path
.libPaths(c("/project/R_libs", .libPaths()))

# Set working directory to project
if (interactive()) {
    setwd("/project")
}

cat("✓ Project ready | Custom lib path active\n")
# cat("✓ Project environment loaded | libs: /project/R_libs | wd: /project\n")
# cat("✓ Project .Rprofile loaded\n")
# cat("Loading project .Rprofile...\n")
